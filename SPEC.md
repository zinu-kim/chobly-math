# 초연이의 수학문제 생성기 — 요구사항 명세

> 이 문서가 유일한 기준이다.
> 여기 없는 것을 임의로 추가하거나, 여기 있는 것을 임의로 바꾸지 않는다.
> 바꿔야 할 이유가 생기면 **아빠에게 먼저 묻고**, 아빠가 결정한 뒤에 이 문서를 고친다.
> 코딩 전에 반드시 이 문서를 먼저 읽는다.

---

## 0. 작업 규칙 (아빠가 지시한 것)

| 규칙 | 내용 |
|---|---|
| 코딩 허락 | **아빠가 "코딩 시작"이라고 하기 전에는 절대 코딩하지 않는다.** |
| 컨셉 변경 | 컨셉은 아빠가 정한다. 임의로 섞거나 바꾸지 않는다. |
| 안내 기준 | 아빠는 지금 **모바일에서만** 작업·테스트한다. PC/터미널 기준으로 설명하지 않는다. PC로 옮길 때 아빠가 알려준다. |
| 이전 채팅 | 기존 클로드 채팅에서 만들던 것은 주문이 꼬였으므로 **참조하지 않는다.** |
| 안 되는 것 | 안 되면 "안 된다"고 명확히 말하고 **차선을 아빠가 고르게 한다.** 혼자 대안을 정하지 않는다. |

---

## 1. 프로젝트 개요

- **앱 이름**: 초연이의 수학문제 생성기
- **대상**: 초등학교 5학년 딸 초연이
- **구성**: 앱 2개
  - **아빠 앱** — 문제 생성
  - **초연이 앱** — 문제 풀기
- **비용**: **API 안 쓴다. 완전 무료 구조.**

### 기기 / 배포

| 항목 | 내용 |
|---|---|
| 아빠 폰 | 갤럭시 폴드6 — **펼친 화면·접은 화면 둘 다** 줄 안 맞거나 깨지는 것 없어야 함 |
| 초연이 폰 | 갤럭시 S21 (안드로이드) — 동일 |
| 설치 | 홈 화면에 앱처럼 설치(PWA). 안 되는 구조면 웹 즐겨찾기도 허용 |
| 주소 | **주소 2개로 분리해서 고정 사용.** 앱을 업데이트해도 기존 주소 그대로 유지 |
| 버전 | 주소 유지가 어려우면 `_01` 식으로 숫자를 올린다. 이 경우 **두 앱의 숫자를 맞춘다** |

---

## 2. 아빠 앱 — 문제 생성

### 흐름

| 단계 | 이름 | 내용 |
|---|---|---|
| ① | **사진 넣기** | 초연이가 카톡으로 보낸 문제 사진을 폰에 받아서 앱에 넣는다 |
| ② | **텍스트 확인** | 앱이 사진을 글자로 인식해서 보여준다 → 아빠가 확인 |
| ③ | **요청 만들기** | 확정된 텍스트로 프롬프트를 만들어 복사 → 외부 AI에 붙여넣고 답변을 받아온다 |
| ④ | **문제 고르기** | 생성된 문제를 조회하고, 보낼 것만 선택 |
| ⑤ | **내보내기** | 고른 것만 코드화 → 초연이 카톡으로 전송 |

### ② 텍스트 확인 — 상세

- 사진에서 인식한 글자를 앱 화면에 보여준다.
- **분수는 `1/2`처럼 쓰지 않고 진짜 분수처럼 세로로 표기한다.**
- 아빠의 선택지 3가지:
  - **맞습니다** → ③으로 진행
  - **다시 인식** → 사진을 다시 읽는다
  - **직접 수정** → 아빠가 텍스트를 직접 고쳐서 확정
- **이 단계에서 [맞습니다]를 누르기 전에는 ③으로 못 넘어간다.**
- 이 단계는 **앱 안에서 처리한다. 외부 AI를 부르지 않는다.**

### ③ 요청 만들기 — 상세

- **풀이 포함 체크박스** — 켜면 풀이까지 같이 받아온다. 끄면 안 받는다. 아빠가 그때그때 고른다.
- **Claude / ChatGPT 전환** — 클로드 사용량을 다 쓴 날 대비. 고른 쪽에 맞게 프롬프트 문구만 바뀐다.
- 프롬프트 복사 → 아빠가 해당 앱 창에 붙여넣기 → 답변을 그대로 복사해서 앱에 붙여넣기.
- **사진은 여기서 다시 넣지 않는다.** ②에서 확정한 텍스트만 나간다.

### ④ 문제 고르기 — 상세

- 생성된 문제를 **문제 + 정답**(풀이 포함을 켰으면 풀이까지) 조회한다.
- **체크박스로 초연이에게 보낼 것만 선택한다.**
- **AI가 낸 정답이 맞는지는 아빠가 눈으로 보고 컨펌한다.**

---

## 3. 초연이 앱 — 문제 풀기

| 단계 | 내용 |
|---|---|
| ① | 홈 |
| ② | 아빠가 보낸 코드를 전체 복사해서 붙여넣고 **불러오기** |
| ③ | 문제를 **순서대로** 하나씩 푼다 |
| ④ | 결과 확인 |

### 채점 규칙

- 틀리면 **다시 풀 기회를 1번** 준다.
- 그래도 틀리면 **정답과 풀이를 보여준다.**
- 초연이 결과는 **아빠에게 피드백하지 않는다.**

### 다시 풀기

- 다 맞히지 못한 문제는 **그 문제만 다시 풀 수 있다.**
- 다 맞았더라도 원하면 **처음부터 순서대로 다시** 풀 수 있다.

---

## 4. 정답 판정 규칙

| 항목 | 규칙 |
|---|---|
| 값 | **값만 맞으면 정답.** |
| 분수 | **약분 안 한 답도 정답.** (예: 10/24 = 5/12) |
| 대분수 | 대분수를 원한 문제에 **정수칸 비우고 가분수로 넣어도, 값이 같으면 정답.** |
| 단위 | 정답란에 **단위까지 적어야 정답 처리.** → 구현은 10절 4번 참고 (단위를 옆에 붙여 보여주고 숫자만 입력) |
| 단위 선택 | 문제가 리터로 서술되었으면 답도 가급적 리터. 단 결과가 `23000mL`처럼 나오면 **리터로 쓰도록 유도한다.** |

---

## 5. 입력 / 표시 위젯

**`1/2` 같은 텍스트 표기를 쓰지 않는다. 보이는 것도, 입력하는 것도 실제 수학 표기 모양이어야 한다.**

| 형태 | 표시 | 입력 |
|---|---|---|
| 분수 | 진짜 분수 모양 (세로) | 가로줄 위아래로 빈칸 |
| 대분수 | 정수 + 분수 | 앞에 정수 한 칸, 뒤에 가로줄 위아래 칸 |
| 소수 | — | `.` 을 중심으로 좌우 칸 |
| 정수 | — | 한 칸 |

---

## 6. 문제 생성 규칙

| 항목 | 규칙 |
|---|---|
| 개수 | **항상 5개.** 늘리지 않는다. |
| 난이도 | 원본과 **동일 수준.** |
| 유형 | **같은 유형**을 유지하되 폭은 다양하게. |
| 소재 | 원본이 점수·평균이면, 생성 문제는 **물건의 양 / 강수량 / 생산량 / 판매량** 등 **다양한 소재**를 갖다 쓴다. 숫자도 조금씩 바꾼다. |
| 과욕 금지 | **큰 틀에서 문제를 변형하려다 오버해서 오류를 내지 않는다.** (구조를 뒤집는 식의 변형 금지) |
| 정답 형태 | 정수로 떨어지면 좋지만 **거기에 연연하지 않는다.** 안 나눠떨어지는 수만 아니면 **소수점 2~3자리까지 가도 된다.** |
| 최우선 | **정답이 정확한 것이 가장 중요하다.** |
| 풀이 | 수학 증명하듯 **순서대로 설명**하고, 마지막은 **"따라서 ~~~"** 로 마무리 |
| 도형 그림 | **배제한다.** |
| 표 | 표가 있는 문제는 인식·생성 모두 표까지 포함하면 좋다. **단 구현이 복잡해지면 보류.** |

---

## 7. 이력 관리

- 아빠 앱·초연이 앱 **각각 최근 5개**까지 생성 문제 이력을 남긴다.
- 6번째를 만들면 **맨 아래가 자동으로 사라진다.**
- **신규 생성 문제는 파란색으로 구분한다.**

---

## 8. 공유 방식

- 아빠 앱에서 고른 문제를 **코드화한 텍스트**로 만든다.
- **초연이 카톡으로 바로 보내기** 기능을 연결한다.
- 초연이는 받은 코드를 **전체 복사** → 앱에서 **불러오기** → 붙여넣기 → 문제 풀이 시작.

---

## 9. 논의를 거쳐 최종 확정된 항목

처음에 열어뒀거나 중간에 다시 검토한 것들의 **현재 확정 상태**다. 위 본문이 이 결정을 반영하고 있다.

| # | 쟁점 | 최종 결정 |
|---|---|---|
| 1 | 사진 인식(OCR)을 어디서 하나 | **앱 안에서 한다.** 인식·확인 단계에서는 외부 AI를 부르지 않는다. |
| 2 | 문제 생성을 앱 안에서 할 수 있나 | **불가능하다고 확인됨.** 앱은 글자를 읽을 수는 있어도 문제 유형을 이해하지 못한다. → **프롬프트를 복사해 외부 AI(클로드/챗지피티) 창에 붙여넣고 답변을 받아오는 방식**으로 확정. |
| 3 | 사진을 두 번 넣는 문제 | **없앤다.** 사진은 ①에서만 쓴다. ③에는 ②에서 확정한 **텍스트만** 나간다. |
| 4 | 풀이 기능을 넣을지 | **체크박스로 아빠가 그때그때 선택.** (처음엔 보류였다가 이렇게 확정) |
| 5 | 한 번에 생성할 문제 수 | **5개 고정.** 실패율을 이유로 더 받지 않는다. 실패율은 개선으로 푼다. |
| 6 | 정답이 정수로 안 떨어질 때 | **정수에 연연하지 않는다.** 소수점 2~3자리 허용. 정확성이 우선. |
| 7 | AI가 낸 정답을 누가 검증하나 | **아빠가 눈으로 보고 컨펌.** |
| 8 | 클로드 사용량이 떨어지면 | **챗지피티로 우회.** 프롬프트 문구만 바꿔서 내보낸다. |
| 9 | 앱을 하나로 할지 둘로 할지 | **주소 2개로 분리.** |

---

## 10. 미정이었던 것 — 이렇게 가정하고 만들었음 (아빠 확인 필요)

> 코딩을 진행하기 위해 아래처럼 가정했다. **틀리면 말해주면 바꾼다.**

| # | 쟁점 | 이번에 이렇게 만듦 |
|---|---|---|
| 1 | 원본 문제의 정답을 누가 푸나 | ③의 요청 한 번에 **"원본 문제 풀이 + 유사문제 5개"** 를 같이 시킨다. ④ 화면 맨 위에 원본 정답을 따로 보여주고 아빠가 같이 확인한다. |
| 2 | 검산을 프롬프트에 넣을지 | **넣었다.** "만든 문제를 직접 풀어서 검산하고, 지저분한 소수가 나오면 숫자를 바꿔 다시 만들라"는 지시가 들어간다. |
| 3 | 표 지원 | **보류로 시작.** 표는 인식·생성 모두 넣지 않았다. |
| 4 | 단위를 초연이가 직접 타이핑하나 | **아니다.** 단위(kg, mm 등)는 입력칸 옆에 **글자로 붙여서 보여주고**, 초연이는 그 단위 기준의 숫자만 넣는다. 5학년이 모바일에서 단위까지 타이핑하는 건 번거롭다고 판단했다. |

---

## 11. 만들면서 확인한 한계

- **글자 인식(OCR)은 Tesseract 라이브러리로 앱 안에서 돌린다.** 인쇄된 글씨는 잘 읽지만, **손글씨가 겹쳐 있거나 사진이 흐리면 잘 못 읽는다.** 그래서 ②단계는 인식 결과를 **바로 고칠 수 있는 입력칸**으로 만들었고, 인식이 실패해도 직접 타이핑해서 그대로 진행할 수 있다.
- **처음 한 번은 한글 인식 데이터를 내려받느라 시간이 걸린다.** (약 15MB, 이후에는 저장돼서 빨라진다)
- 아빠 앱은 인터넷이 필요하다. **초연이 앱은 문제를 한 번 받아두면 인터넷 없이 돌아간다.**

---

*최종 갱신: 2026-08-21*
