# CLAUDE.md

이 저장소에는 서로 무관한 앱 두 개가 있다.

| 경로 | 앱 |
|---|---|
| `/`, `kid/`, `parent/`, `shared/`, `mockup/`, `SPEC.md` | 초블리 수학 문제 생성기 (기존) |
| `fridge/`, `FRIDGE_SPEC.md` | 하야의 냉장고 (신규) |

냉장고 작업 중에는 수학 앱 파일을 건드리지 않는다. `shared/` 는 수학 앱 전용이며 냉장고가 공유하지 않는다.

---

# 하야의 냉장고

아내가 혼자 쓰는 요리 추천 앱. 냉장고 재고는 힌트일 뿐이고 **레시피 추천이 앱의 본체**다.
요구사항 원본은 `FRIDGE_SPEC.md` 이며, 이 문서와 충돌하면 `FRIDGE_SPEC.md` 가 우선한다.

## 절대 제약

설계 판단이 갈릴 때 아래 순서로 우선한다.

1. **완전 무료** — 유료 API, 유료 호스팅, 구독 결제 일절 없음
2. **아내 입장에서 모든 처리는 앱 내부에서 일어나는 것으로 보여야 함**
   외부 앱으로 튕기거나, 복사·붙여넣기를 요구하거나, 긴 로딩이 뜨는 흐름은 실패로 간주한다
3. **혼자 쓰는 앱** — 계정·로그인·권한·동기화·서버 없음. 상태는 localStorage
4. **오프라인 동작** — 네트워크 없어도 재고 관리와 레시피 열람이 되어야 함

**런타임 LLM 호출을 하지 않는다.** 추천은 내장된 레시피 1000개를 태그로 필터링해 만든다.
API 키를 코드에 넣는 제안도 하지 않는다.

## 확장성을 만들지 않는다

레시피 1000개 고정, 사용자 1명 고정이다. 지금 필요 없는 구조는 넣지 않는다.

- 빌드 스텝 없음, npm 의존성 없음, 프레임워크 없음
- 페이지네이션·가상 스크롤·IndexedDB·샤딩 전부 불필요
- 상태 관리 라이브러리 없이 평범한 객체 하나로 관리한다

## 기술 스택

- 단일 HTML + 인라인 CSS/JS (`fridge/index.html`)
- 레시피 데이터만 별도 파일 (`fridge/recipes.json`, 약 1.2 MB)
- PWA — `fridge/manifest.json`, `fridge/sw.js`
- GitHub Pages 배포
- 외부 로드는 Google Fonts 뿐 (Gowun Batang + IBM Plex Sans KR)

## 레시피 데이터 규칙

`fridge/recipes.json` 의 각 항목은 아래 필드를 **전부** 갖춘다. 하나라도 비우지 않는다.

`id` / `name` / `cuisine` / `situation` / `type` / `flavor` / `main` /
`time` / `servings` / `calories` / `nutrition` / `ingredients` / `steps`

### 태그는 닫힌 어휘다 — 목록에 없는 값을 새로 만들지 않는다

| 필드 | 개수 | 허용값 |
|---|---|---|
| `cuisine` | 1개 필수 | 한식 · 양식 · 중식 · 일식 · 베트남식 · 태국식 · 간식 |
| `situation` | 0개 이상 | 술안주 · 건강식 · 아이들요리 · 간편식 |
| `type` | 1개 필수 | 밥 · 국찌개 · 볶음 · 무침 · 조림 · 구이 · 튀김 · 면 · 샐러드 · 빵디저트 · 음료 |
| `flavor` | 1~2개 | 매콤 · 얼큰 · 담백 · 고소 · 새콤 · 달콤 · 짭짤 |
| `main` | 1~3개 | 자유. 단 **일반명** (칵테일새우 ✗ → 새우 ○) |

태그 어휘가 흔들리면 추천 품질이 무너진다. 레시피 개수보다 태그 일관성이 중요하다.

### 그 밖의 데이터 규칙

- `main` 은 재고와 매칭되는 축이므로 일반명. 구체 품목명과 용량은 `ingredients` 에만
- **모든 재료에 용량 표기 필수** (애호박 1개, 새우 150g, 간장 1큰술)
- `steps` 는 문장형, 단계당 한 문장, 4~8단계
- `ingredients` 는 3~12개
- 요리명은 1000개 전체에서 중복되지 않는다
- 계열 비율: 한식 420 / 양식 140 / 중식 110 / 일식 110 / 베트남식 60 / 태국식 50 / 간식 110
- 상황 태그 최소치: 간편식 250 / 술안주 150 / 아이들요리 150 / 건강식 150

## UI 규칙 (어기면 안 됨)

- 홈 카드에는 **요리명 · 조리시간 · 칼로리만**. 재료 충족도 수치 금지
- 홈 추천은 항상 **5개**를 채운다. 조건에 맞는 게 부족하면 조건을 완화해 채운다
- 자연어 입력 결과는 **오늘의 추천 목록을 갈아끼운다**. 레시피 탭으로 자동 이동하지 않는다
- 재고 변경은 홈 추천에 즉시 반영되지 않는다. "다시 추천"을 눌러야 반영
- 재료 보유 표시는 `●` / `○` 뿐. 수량 비교가 아니라 보유 여부만 뜻한다
- 오미자색(`--omija`)은 **저장(♥)과 폐기 확인 다이얼로그에만** 쓴다
- 재고 삭제는 반드시 확인 다이얼로그를 거친다. 추가는 즉시 반영
- 재고 중복 추가 시 "이미 등록된 재료입니다" 표시 후 입력창 초기화
- 재고는 항상 가나다순
- 탭 바와 입력창은 스크롤해도 상단 고정
- 참고 영상은 요리명으로 유튜브 검색을 여는 **버튼 하나**. 링크를 저장하지 않는다
- 즐겨찾기는 레시피 내용을 통째로 저장하되, 열 때 `●`/`○` 만 현재 재고로 다시 판정한다

## 색상 토큰

```css
--paper:      #F5F8F3;
--surface:    #FFFFFF;
--ink:        #1B2620;
--ink-soft:   #6E7D73;
--line:       #E5EBE1;
--green:      #3E7D52;
--omija:      #C4394F;
--amber:      #DD9B33;
```

전체적으로 밝게 유지한다. 1차 목업이 침침하다는 피드백을 받아 조정한 값이므로 더 어둡게 만들지 않는다.

## 톤

- UI 문구·주석·커밋 메시지 전부 한국어
- 과한 이모지나 느낌표 없이 담백하게
- 아내가 읽는 문구에 기술 용어를 노출하지 않는다
