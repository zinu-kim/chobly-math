# 하야의 냉장고 — 기본 컨셉 & UI 구성

## 1. 개요

- **앱 이름**: 하야의 냉장고
- **사용자**: 와이프 단독 사용 (공유·동기화 불필요)
- **목적**: 요리 결정에 드는 고민을 줄이는 것. 냉장고 재고는 "참고 정보"일 뿐이고, **레시피 추천이 앱의 핵심**이다.
- **핵심 흐름**: 재고를 가볍게 등록 → 홈에서 추천을 받거나 자연어로 요청 → 레시피 탭에서 상세 레시피 확인 → 마음에 들면 즐겨찾기에 저장

## 2. 설계 원칙

1. **재고 관리는 최소한으로.** 수량·용량·유통기한은 다루지 않는다. 항목 이름만 등록한다.
2. **재고는 제약이 아니라 힌트.** 재료가 완전히 갖춰지지 않아도 추천을 막지 않는다.
3. **입력은 짧게, 결과는 충분하게.** 등록은 한 번 터치로, 레시피는 바로 요리할 수 있을 만큼 상세하게.
4. **"저장"은 레시피 탭에만 존재한다.** 홈·재고에는 저장 개념이 없다.
5. **와이프 혼자 쓰는 앱.** 계정·권한·공유 개념 없음.

## 3. 화면 구성

상단 고정 탭 4개: **홈 / 재고 / 레시피 / 즐겨찾기**

### 3.1 홈 (탭 1)

- 상단 자연어 입력창: "매콤한 거", "간단한 국물", "애들 반찬" 등 자유 입력
- 추천 카드에 표시하는 정보는 **요리명 / 조리시간 / 칼로리 뿐**
- 카드에 재료 보유 개수(보유 N / 필요 M) 등 충족도 표시는 두지 않는다
- 카드를 누르면 **레시피 탭으로 이동**해 그 요리의 상세 레시피를 받는다
- 홈에는 저장·즐겨찾기 버튼이 없다

### 3.2 재고 (탭 2)

- 입력창에 이름 적고 + 로 추가, 항목 옆 × 로 삭제
- 목록은 가나다순 정렬, 세로 스크롤
- 입력창과 탭 바는 스크롤 시에도 상단에 고정
- + / × 조작이 곧 반영 (별도 저장 버튼 없음)

### 3.3 레시피 (탭 3)

- 홈에서 추천 카드를 누르거나, 즐겨찾기에서 항목을 누르면 이 탭이 열림
- **재료는 용량·수량을 반드시 포함** (예: 애호박 1개, 새우 150g, 간장 1큰술)
- ●/○ 는 냉장고 보유 여부만 표시 (수량 비교 아님)
- 우상단 ♡ 로 즐겨찾기 저장/해제
- 하단 **참고 영상**: 같거나 유사한 메뉴 링크. 없으면 영역 미표시

### 3.4 즐겨찾기 (탭 4)

- 레시피 탭에서 ♡ 저장한 것만
- 저장 역순 나열 (최신 우선)
- 항목 탭 → 레시피 재개, ♡ 다시 누르면 제거

## 4. 동작 규칙

### 재고
- 항목 이름만 저장, 수량·유통기한 없음
- 중복 불추가
- **추가**: 즉시 반영
- **삭제**: "'OOO'을(를) 폐기하시겠습니까?" 확인 후 반영
- 항상 가나다순 정렬
- 재고 변경 시 홈 추천 자동 갱신 안 함 (홈 진입 또는 "다시 추천" 눌렀을 때 갱신)

### 홈 추천
- 재고 기반 제안이지만 재료 부족해도 추천 가능
- 수량 미관리이므로 충족도 수치 계산 안 함
- 재고 비어있으면 일반 가정식 위주 추천

### 자연어 요청
- 요청 내용(맛·종류·상황·시간)이 재고보다 우선
- 재고는 요청 만족 선에서 가급적 활용
- 결과도 홈 추천 카드 형태, 누르면 레시피 탭 이동

### 레시피
- 항상 포함: 요리명 / 재료(용량, 보유여부 구분) / 조리순서 / 시간 / 인분 / 칼로리 / 영양정보
- 보유 여부는 표시 시점 재고 기준
- 참고 영상은 유사 메뉴 허용, 새 창 열림

### 즐겨찾기 저장
- 레시피 전체 저장 (재생성 불필요)
- 다시 열 때 재료 보유 표시만 현재 재고 기준 재판정
