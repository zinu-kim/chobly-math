# 하야의 냉장고 — 요구사항 명세

## 1. 개요

- **앱 이름**: 하야의 냉장고
- **사용자**: 아내 단독 사용. 초기 시험은 개발자 폰, 이후 아내 폰에 설치
- **목적**: 요리 결정에 드는 고민을 줄이는 것. 냉장고 재고는 "참고 정보"일 뿐이고, **레시피 추천이 앱의 핵심**이다
- **핵심 흐름**: 재고를 가볍게 등록 → 홈에서 추천을 받거나 자연어로 요청 → 레시피 탭에서 상세 확인 → 마음에 들면 즐겨찾기에 저장

## 2. 절대 제약

설계 판단이 갈릴 때 아래 순서로 우선한다.

1. **완전 무료** — 유료 API, 유료 호스팅, 구독 결제 일절 없음
2. **아내 입장에서 모든 처리는 앱 내부에서 일어나는 것으로 보여야 함**
   외부 앱으로 튕기거나, 복사·붙여넣기를 요구하거나, 긴 로딩이 뜨는 흐름은 실패로 간주
3. **혼자 쓰는 앱** — 계정·로그인·권한·동기화·서버 없음. 상태는 localStorage
4. **오프라인 동작** — 네트워크 없어도 재고 관리와 레시피 열람이 되어야 함

이 제약 때문에 **런타임 LLM 호출은 하지 않는다.** 추천은 앱에 내장된 레시피를 태그로 필터링해 만든다.

## 3. 설계 원칙

1. **재고 관리는 최소한으로.** 수량·용량·유통기한은 다루지 않는다. 항목 이름만 등록한다
2. **재고는 제약이 아니라 힌트.** 재료가 완전히 갖춰지지 않아도 추천을 막지 않는다
3. **입력은 짧게, 결과는 충분하게.** 등록은 한 번 터치로, 레시피는 바로 요리할 수 있을 만큼 상세하게
4. **"저장"은 레시피 탭에만 존재한다.** 홈·재고에는 저장 개념이 없다
5. **확장성은 고려하지 않는다.** 사용자 1명 고정. 지금 필요 없는 구조는 만들지 않는다

## 4. 화면 구성

상단 고정 탭 4개: **홈 / 냉장고 / 레시피 / 즐겨찾기**

화면에서는 "재고"라는 말을 쓰지 않는다. 탭 이름도 문구도 "냉장고"다. (데이터·코드 안에서는 재고로 부른다.)

### 4.1 홈

- 상단 자연어 입력창: "새우를 활용한 매운 요리", "간단한 국물", "애들 반찬" 등 자유 입력
- **카테고리 칩 두 줄** — 각 줄에서 하나씩 고른다. 두 축은 서로 독립이다
  - `나라` : 자유 · 한식 · 중식 · 일식 · **기타**
  - `자리` : 자유 · 가정식 · 술안주 · 아이들요리 · 간식 · **기타**
  - **자유** = 전 범위 (한중일 포함 모두)
  - **기타** = 그 줄에 이름 붙은 것들을 전부 뺀 나머지
    나라 기타 → 한식·중식·일식이 아닌 것 (양식·베트남식·태국식·인도식 …)
    자리 기타 → 가정식·술안주·아이들요리·간식 어디에도 안 들어가는 것
  - 양쪽 다 **자유가 기본값**. "나라는 자유인데 술안주로 고정" 처럼 한쪽만 잠글 수 있다
  - 칩을 누르면 그 자리에서 추천이 다시 뽑힌다

세계 요리를 나라별로 다 나눌 수 없으므로 화면에는 한·중·일만 세우고
나머지는 `기타` 하나로 묶는다. 데이터에는 실제 나라를 그대로 적어 두므로
"베트남 국수" 같은 자연어 검색은 계속 걸린다.
- **오늘의 추천 카드 5개**
- 카드에 표시하는 정보는 **요리명 / 조리시간 / 칼로리 뿐**. 재료 충족도 수치는 표시하지 않는다
- 카드를 누르면 **레시피 탭으로 이동**해 상세 레시피를 표시
- 하단 "다시 추천" — 누르면 현재 재고 기준으로 5개를 새로 뽑는다
- 홈에는 저장·즐겨찾기 버튼이 없다

### 4.2 냉장고

- 입력창에 이름 적고 `+` 로 추가, 항목 옆 `×` 로 삭제
- 목록은 가나다순 정렬, 세로 스크롤
- 입력창과 탭 바는 스크롤 시에도 상단 고정
- `+` / `×` 조작이 곧 반영 (별도 저장 버튼 없음)

### 4.3 레시피

- 홈에서 추천 카드를 누르거나, 즐겨찾기에서 항목을 누르면 열림
- **재료는 용량·수량을 반드시 포함** (예: 애호박 1개, 새우 150g, 간장 1큰술)
- `●` / `○` 는 냉장고 보유 여부만 표시. 수량 비교가 아니다
- 우상단 `♡` 로 즐겨찾기 저장/해제
- 하단 **참고 영상** — 요리명으로 유튜브를 검색하는 버튼 1개

### 4.4 즐겨찾기

- 레시피 탭에서 `♡` 저장한 것만
- 저장 역순 나열 (최신 우선)
- 항목을 누르면 레시피 탭으로, `♡` 다시 누르면 제거

## 5. 동작 규칙

### 냉장고

- 항목 이름만 저장. 수량·유통기한 없음
- **중복**: 이미 있는 이름을 추가하면 "이미 등록된 재료입니다" 표시 후 입력창 초기화
- **추가**: 즉시 반영
- **삭제**: `OOO를 냉장고에서 뺄까요?` 확인 다이얼로그를 거친 후 반영 (버튼: 그대로 두기 / 빼기)
  받침에 따라 조사를 고른다 — 대파**를** / 달걀**을**
- 항상 가나다순 정렬
- **냉장고를 바꿔도 홈 추천은 자동 갱신하지 않는다.** 홈에서 "다시 추천"을 눌렀을 때부터 반영된다

### 홈 추천

- 재고 기반 제안이지만 재료가 부족해도 추천한다
- 수량을 관리하지 않으므로 충족도 수치는 계산하지 않는다
- 재고가 비어 있으면 일반 가정식 위주로 추천
- **추천이 비는 경우는 없다.** 항상 5개를 채운다
- 조건을 아래 순서로 한 단계씩 풀어가며 채운다 — 최대한 가까운 것부터 붙인다

  | 단계 | 조건 |
  |---|---|
  | 1 | 칩 양쪽 + 자연어 모두 만족 |
  | 2 | 칩 양쪽만 만족 |
  | 3 | 칩 한쪽 + 자연어 만족 |
  | 4 | 칩 한쪽만 만족 |
  | 5 | 자연어만 만족 |
  | 6 | 전부 |

- 칩과 자연어는 함께 걸린다. 예: `한식` + `간식` + "매운"

### 추천 안내 문구

1단계로 5개를 다 못 채웠거나 자연어를 못 알아들었으면 추천 위에 한 줄을 띄운다.

| 상황 | 문구 |
|---|---|
| 자연어에서 아무 조건도 못 건짐 | `무슨 요리인지 몰라서 비슷한 걸로 골라봤어요` |
| 조건에 맞는 게 5개 미만 | `딱 맞는 요리가 없어 비슷한 걸로 채웠어요` |
| 1단계로 다 채움 | 문구 없음 |

문구를 띄우더라도 카드 5장은 그대로 채운다. 추천을 못 받는 경우를 만들지 않는다.

### 자연어 요청

- 요청 내용(맛·재료·종류·상황·시간)이 재고보다 우선
- 재고는 요청을 만족하는 선에서 가급적 활용
- **결과는 오늘의 추천 목록을 그 기준으로 갈아끼우는 방식.** 레시피 탭으로 자동 이동하지 않는다
- 카드를 눌러야 레시피 탭으로 넘어간다

### 레시피

- 항상 포함: 요리명 / 재료(용량 포함, 보유 여부 구분) / 조리순서 / 조리시간 / 인분 / 칼로리 / 영양정보
- 보유 여부는 표시 시점의 재고 기준
- **참고 영상**: 요리명으로 유튜브 검색을 여는 버튼 하나
  `https://www.youtube.com/results?search_query={요리명}+레시피`
  링크를 저장하지 않으므로 죽은 링크가 생기지 않고 항상 최신 영상이 잡힌다
  "추천 영상이 없습니다" 상태는 존재하지 않는다

### 재료 보유 판정

- 재고 항목명이 재료명에 포함되면 보유(`●`)로 본다
  예: 재고 `새우` → 재료 `칵테일새우 150g` 은 보유
- 재고 항목이 1글자면 완전일치만 인정한다 (`파` 가 `파프리카` 에 걸리는 것을 막기 위함)

### 즐겨찾기

- 레시피 내용 전체를 저장한다 (재생성 불필요)
- **다시 열 때 재료 보유 표시(`●`/`○`)는 현재 재고 기준으로 다시 판정한다**
- 영상은 저장 대상이 아니다. 요리명으로 매번 검색 링크를 만든다

## 6. 레시피 데이터

`fridge/recipes.json` 에 내장한다.

### 6.0 개수는 목표가 아니라 결과다

**목표 개수를 정해두고 채우지 않는다.** 할당량을 채우려는 순간 없는 요리를 지어내게 되고,
지어낸 요리는 목록만 봐서는 아무도 걸러낼 수 없다.

- **실제로 존재하는 요리만 넣는다.** 이건 타협하지 않는다
- 계열별로 실존 요리가 바닥나면 **거기서 멈춘다.** 300개든 600개든 그게 맞는 숫자다
- 판별 기준은 읽고 판단하는 것이 아니라 **요리명으로 검색해서 실제 레시피가 나오는가**다
  앱이 이미 요리명으로 유튜브를 여니, 검색해서 안 나오는 요리는 애초에 들어가면 안 된다
- 애매하면 뺀다. 600개짜리가 900개짜리보다 낫다

조리 순서나 투입량은 다소 부정확해도 된다. **없는 요리를 안내하는 것만 안 된다.**

### 6.1 스키마

```json
{
  "id": "k0001",
  "name": "애호박 새우볶음",
  "cuisine": "한식",
  "situation": ["간편식"],
  "type": "볶음",
  "flavor": ["담백"],
  "main": ["애호박", "새우"],
  "time": 20,
  "servings": 2,
  "calories": 310,
  "nutrition": { "carb": 24, "protein": 18, "fat": 12 },
  "ingredients": [
    { "name": "애호박", "amount": "1개" },
    { "name": "칵테일새우", "amount": "150g" }
  ],
  "steps": [
    "애호박은 반달 모양으로 0.5cm 두께로 썬다.",
    "팬에 기름을 두르고 다진 마늘을 약불에 볶아 향을 낸다."
  ]
}
```

모든 필드는 필수다. 하나라도 비우지 않는다.

### 6.2 태그 어휘 (닫힌 목록 — 여기 없는 값을 쓰지 않는다)

| 필드 | 개수 | 허용값 |
|---|---|---|
| `cuisine` | 1개 필수 | 한식 · 중식 · 일식 · 양식 · 베트남식 · 태국식 · 인도식 |
| `situation` | 0개 이상 | 가정식 · 술안주 · 아이들요리 · 간식 |
| `type` | 1개 필수 | 밥 · 국찌개 · 볶음 · 무침 · 조림 · 구이 · 튀김 · 면 · 샐러드 · 빵디저트 · 음료 |
| `flavor` | 1~2개 | 매콤 · 얼큰 · 담백 · 고소 · 새콤 · 달콤 · 짭짤 |
| `main` | 1~3개 | 자유. 단 **일반명**으로 적는다 (칵테일새우 ✗ → 새우 ○) |

`main` 은 재고와 매칭되는 축이므로 일반명을 쓰고, 구체적인 품목명과 용량은 `ingredients` 에만 적는다.

`situation` 은 **저장하는 태그가 4개뿐**이다. 화면의 `기타` 칩은 저장된 값이 아니라
"이 4개 중 아무것도 안 붙은 것"을 그때그때 계산해 보여준다. `나라` 의 `기타` 도 같다.
`간편식` 은 태그로 두지 않고 `time` 으로 판단한다 ("10분 안에", "간단한" → 15분 이하).

### 6.3 계열별 예상 규모 (목표가 아니라 전망)

실존 요리만 넣었을 때 대략 이 근처에서 바닥날 것으로 본다. **채워야 할 숫자가 아니다.**
실제로 해보고 더 나오면 더 넣고, 덜 나오면 덜 넣는다.

| 계열 | 예상 | 화면의 칩 |
|---|---|---|
| 한식 | ~320 | 한식 |
| 양식 | ~130 | 기타 |
| 일식 | ~70 | 기타는 아님 (일식 칩) |
| 중식 | ~60 | 중식 |
| 베트남식 | ~15 | 기타 |
| 태국식 | ~15 | 기타 |
| 인도식 | ~15 | 기타 |
| **합계** | **~625** | |

중식이 가장 위험하다. 한국에서 실제로 해먹는 중화요리는 60개 근처에서 끝나므로
그 이상을 요구하면 지어내게 된다.

간식은 나라가 아니므로 계열이 아니라 자리 축에 둔다. 감자전은 `한식` + `[간식, 술안주]`,
바나나 팬케이크는 `양식` + `[간식, 아이들요리]` 처럼 붙는다.

### 6.4 자리 태그

자리 태그는 계열과 무관하게 겹쳐 붙는다. **최소 개수를 강제하지 않는다** —
숫자를 맞추려고 억지로 태그를 붙이면 필터가 망가진다. 요리 성격대로 붙이고
결과로 나오는 분포를 받아들인다.

다만 다섯 칩이 모두 뭔가를 보여줘야 하므로, 한쪽이 지나치게 비면 그때 보충한다.
`기타` 칩(넷 다 아닌 것)은 샐러드·샌드위치·브런치류가 자연히 채운다.

조리 15분 이하 요리는 넉넉히 둔다 (간편식 태그 대신 `time` 으로 판단하므로).

### 6.5 생성 규칙

- 요리명은 전체에서 중복되지 않는다
- `steps` 는 문장형, 단계당 한 문장, 4~8단계
- `ingredients` 는 3~12개, 전부 용량 표기
- **재료량은 1인분 표준 범위로 잡는다.** 후하게 잡으면 칼로리가 통째로 부풀어 오른다
  - 고기·생선 1인분 120~150g / 밥 1공기 210g / 마른 파스타 1인분 80~100g
  - 볶음 기름 1~2큰술 (3큰술은 과하다) / 생크림 1인분 50~80ml

### 6.6 영양정보는 계산해서 넣는다

**칼로리와 탄단지를 손으로 적지 않는다.** `fridge/tools/nutrition.py` 로 계산한다.

1. 요리명이 **식약처 식품영양성분 DB**(`fridge/tools/dish_nutrition_db.csv`, 조리된 음식
   19,495건)와 정확히 일치하면 그 실측값을 쓴다. 1인분 무게는 레시피 재료 총량을
   인분 수로 나눠 정하고, DB의 100g당 값을 그 무게에 비례시킨다
2. 일치하는 이름이 없으면 재료별 100g당 영양가 × 용량을 더해 추정한다 (`NUTRI` 표)

1번이 훨씬 정확하다. 미역국처럼 물을 졸이는 요리는 재료 합산으로 추정하면
실측치의 3배 넘게 부풀 수 있다 — 조리 손실은 재료 목록에 드러나지 않기 때문이다.
지금 40개 중 19개가 실측값으로 매칭됐다.

- 결과는 10 단위로 반올림한다. 어림값임을 숫자 모양으로 드러내기 위함이다
- 튀김 기름은 붓는 양이 아니라 **흡수량(재료 무게의 10%)** 으로 계산한다
- 표에 없는 재료가 있으면 계산이 실패하도록 두었다. 조용히 0 으로 넘어가지 않는다

값 자체는 어림값이다. 완성품 칼로리는 공개 출처끼리도 2배씩 차이가 나므로
절대적으로 맞출 수 없다. 다만 **"무엇에 근거했는지"** 는 항상 남는다.
자세한 매칭 규칙은 `fridge/tools/README.md` 를 본다.

### 재료는 동네 마트에서 구할 수 있어야 한다

**특수 재료를 쓰는 요리는 넣지 않는다.**

- 넣지 않음: 샥스핀 · 푸아그라 · 트러플 · 샤프란 · 캐비어 · 랍스터 · 와규 ·
  타마린드 페이스트 · 레몬그라스 처럼 대형마트에도 잘 없는 것
- 넣어도 됨: 두반장 · 굴소스 · 피시소스 · 코코넛밀크 · 카레가루 처럼
  대형마트 코너에 상시 있는 것
- 애매하면 뺀다. 아내가 장 보러 갔다가 못 찾는 상황을 만들지 않는다

## 7. 디자인

### 7.1 색상 토큰

목업 1차안이 전반적으로 어두워서 밝게 조정한 값이다.

```css
--paper:      #F5F8F3;   /* 배경 */
--surface:    #FFFFFF;   /* 카드·패널 */
--ink:        #1B2620;   /* 본문 */
--ink-soft:   #6E7D73;   /* 보조 텍스트 */
--line:       #E5EBE1;   /* 구분선·테두리 */
--green:      #3E7D52;   /* 구조·보유 표시 */
--omija:      #C4394F;   /* 저장(♥)·폐기 확인 전용 */
--amber:      #DD9B33;   /* 조리순서 번호 */
```

### 7.2 서체

- 제목·요리명: `Gowun Batang` (700)
- 본문·UI: `IBM Plex Sans KR` (300/400/500/600)

### 7.3 표기 규칙 (어기지 않는다)

- 홈 카드는 **요리명 · 조리시간 · 칼로리만**. 재료 충족도 수치 금지
- `●`/`○` 는 보유 여부만 뜻한다. 수량 비교가 아니다
- 오미자색은 **저장(♥)과 폐기 확인 다이얼로그에만** 쓴다
- 탭 바와 입력창은 스크롤해도 상단 고정
- 냉장고 목록은 항상 가나다순
- 선택된 칩은 초록 배경, 나머지는 흰 배경에 회색 글씨
- 고른 칩이 줄 밖으로 밀리면 가로 스크롤을 맞춰 항상 보이게 한다
- 추천 안내 문구는 회색 톤. 경고처럼 보이게 하지 않는다

## 8. 배포와 설치

### 파일

```
fridge/
  index.html               앱 전체 (인라인 CSS/JS)
  recipes.json             레시피 데이터
  tools/nutrition.py       영양정보 계산 (앱에 포함되지 않음)
  manifest.json            설치 정보
  sw.js                    오프라인 캐시
  icon-192.png             홈 화면 아이콘
  icon-512.png             스플래시·스토어용
  icon-maskable-512.png    안드로이드 마스크용 (안전영역 안쪽 70%)
  apple-touch-icon.png     아이폰 홈 화면용 (180px)
```

### 아이콘

초록 바탕(`--green`)에 흰 냉장고. 홈 화면에서 48dp로 줄어도 알아볼 수 있게
디테일을 문 손잡이 두 개로만 둔다.

### 설치

1. GitHub Pages 로 배포 (무료). 주소는 `https://<계정>.github.io/chobly-math/fridge/`
2. 아내 폰 크롬에서 그 주소를 연다
3. 메뉴 → **홈 화면에 추가** → 아이콘이 생긴다
4. 주소창 없이 전체 화면으로 뜬다 (`display: standalone`)

### 오프라인

서비스워커가 앱·레시피·웹폰트를 캐시에 담는다. 비행기 모드에서도
추천·레시피 열람·냉장고 편집이 그대로 된다. 상태는 localStorage 라 앱을
닫았다 열어도 남는다. 동기화는 없다.

레시피를 새로 푸시하면 캐시를 먼저 보여준 뒤 뒤에서 새 파일을 받아 두므로
**다음 실행 때 반영된다.** 아내가 뭘 누를 필요는 없다.

## 9. 남은 작업

- **레시피 확충** — 현재 `recipes.json` 에는 40개가 들어 있다.
  6.0 원칙(실존 요리만, 바닥나면 멈춤)에 따라 계열별로 채운다.
  요리명 목록을 먼저 뽑아 **검색으로 실존을 확인**한 뒤 본문을 만든다.
  가장 위험한 중식부터 확인하는 것이 좋다
