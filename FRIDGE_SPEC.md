# 하야의 냉장고 — 요구사항 명세

## 1. 개요

- **앱 이름**: 하야의 냉장고
- **사용자**: 아내 단독 사용. 초기 시험은 개발자 폰, 이후 아내 폰에 설치
- **목적**: 요리 결정에 드는 고민을 줄이는 것. 냉장고 재고는 "참고 정보"일 뿐이고, **레시피 추천이 앱의 핵심**이다
- **핵심 흐름**: 재고를 가볍게 등록 → 홈에서 추천을 받거나 자연어로 요청 → 레시피 탭에서 상세 확인 → 마음에 들면 즐겨찾기에 저장

## 2. 절대 제약

설계 판단이 갈릴 때 아래 순서로 우선한다.

1. **완전 무료** — 유료 API, 유료 호스팅, 구독 결제 일절 없음
2. **아내 입장에서 모든 처리는 앱 내부에서 일어나는 것으로 보여야 함**
   외부 앱으로 튕기거나, 복사·붙여넣기를 요구하거나, 긴 로딩이 뜨는 흐름은 실패로 간주
3. **혼자 쓰는 앱** — 계정·로그인·권한·동기화·서버 없음. 상태는 localStorage
4. **오프라인 동작** — 네트워크 없어도 재고 관리와 레시피 열람이 되어야 함

이 제약 때문에 **런타임 LLM 호출은 하지 않는다.** 추천은 앱에 내장된 레시피 1000개를 태그로 필터링해 만든다.

## 3. 설계 원칙

1. **재고 관리는 최소한으로.** 수량·용량·유통기한은 다루지 않는다. 항목 이름만 등록한다
2. **재고는 제약이 아니라 힌트.** 재료가 완전히 갖춰지지 않아도 추천을 막지 않는다
3. **입력은 짧게, 결과는 충분하게.** 등록은 한 번 터치로, 레시피는 바로 요리할 수 있을 만큼 상세하게
4. **"저장"은 레시피 탭에만 존재한다.** 홈·재고에는 저장 개념이 없다
5. **확장성은 고려하지 않는다.** 레시피 1000개 고정. 사용자 1명 고정. 지금 필요 없는 구조는 만들지 않는다

## 4. 화면 구성

상단 고정 탭 4개: **홈 / 재고 / 레시피 / 즐겨찾기**

### 4.1 홈

- 상단 자연어 입력창: "새우를 활용한 매운 요리", "간단한 국물", "애들 반찬" 등 자유 입력
- **오늘의 추천 카드 5개**
- 카드에 표시하는 정보는 **요리명 / 조리시간 / 칼로리 뿐**. 재료 충족도 수치는 표시하지 않는다
- 카드를 누르면 **레시피 탭으로 이동**해 상세 레시피를 표시
- 하단 "다시 추천" — 누르면 현재 재고 기준으로 5개를 새로 뽑는다
- 홈에는 저장·즐겨찾기 버튼이 없다

### 4.2 재고

- 입력창에 이름 적고 `+` 로 추가, 항목 옆 `×` 로 삭제
- 목록은 가나다순 정렬, 세로 스크롤
- 입력창과 탭 바는 스크롤 시에도 상단 고정
- `+` / `×` 조작이 곧 반영 (별도 저장 버튼 없음)

### 4.3 레시피

- 홈에서 추천 카드를 누르거나, 즐겨찾기에서 항목을 누르면 열림
- **재료는 용량·수량을 반드시 포함** (예: 애호박 1개, 새우 150g, 간장 1큰술)
- `●` / `○` 는 냉장고 보유 여부만 표시. 수량 비교가 아니다
- 우상단 `♡` 로 즐겨찾기 저장/해제
- 하단 **참고 영상** — 요리명으로 유튜브를 검색하는 버튼 1개

### 4.4 즐겨찾기

- 레시피 탭에서 `♡` 저장한 것만
- 저장 역순 나열 (최신 우선)
- 항목을 누르면 레시피 탭으로, `♡` 다시 누르면 제거

## 5. 동작 규칙

### 재고

- 항목 이름만 저장. 수량·유통기한 없음
- **중복**: 이미 있는 이름을 추가하면 "이미 등록된 재료입니다" 표시 후 입력창 초기화
- **추가**: 즉시 반영
- **삭제**: `'OOO'을(를) 폐기하시겠습니까?` 확인 다이얼로그를 거친 후 반영
- 항상 가나다순 정렬
- **재고를 바꿔도 홈 추천은 자동 갱신하지 않는다.** 홈에서 "다시 추천"을 눌렀을 때부터 반영된다

### 홈 추천

- 재고 기반 제안이지만 재료가 부족해도 추천한다
- 수량을 관리하지 않으므로 충족도 수치는 계산하지 않는다
- 재고가 비어 있으면 일반 가정식 위주로 추천
- 항상 5개를 채운다. 조건에 맞는 것이 5개 미만이면 조건을 완화해 채운다

### 자연어 요청

- 요청 내용(맛·재료·종류·상황·시간)이 재고보다 우선
- 재고는 요청을 만족하는 선에서 가급적 활용
- **결과는 오늘의 추천 목록을 그 기준으로 갈아끼우는 방식.** 레시피 탭으로 자동 이동하지 않는다
- 카드를 눌러야 레시피 탭으로 넘어간다

### 레시피

- 항상 포함: 요리명 / 재료(용량 포함, 보유 여부 구분) / 조리순서 / 조리시간 / 인분 / 칼로리 / 영양정보
- 보유 여부는 표시 시점의 재고 기준
- **참고 영상**: 요리명으로 유튜브 검색을 여는 버튼 하나
  `https://www.youtube.com/results?search_query={요리명}+레시피`
  링크를 저장하지 않으므로 죽은 링크가 생기지 않고 항상 최신 영상이 잡힌다
  "추천 영상이 없습니다" 상태는 존재하지 않는다

### 재료 보유 판정

- 재고 항목명이 재료명에 포함되면 보유(`●`)로 본다
  예: 재고 `새우` → 재료 `칵테일새우 150g` 은 보유
- 재고 항목이 1글자면 완전일치만 인정한다 (`파` 가 `파프리카` 에 걸리는 것을 막기 위함)

### 즐겨찾기

- 레시피 내용 전체를 저장한다 (재생성 불필요)
- **다시 열 때 재료 보유 표시(`●`/`○`)는 현재 재고 기준으로 다시 판정한다**
- 영상은 저장 대상이 아니다. 요리명으로 매번 검색 링크를 만든다

## 6. 레시피 데이터

`fridge/recipes.json` 에 **1000개**를 내장한다. 고정이며 늘리지 않는다.

### 6.1 스키마

```json
{
  "id": "k0001",
  "name": "애호박 새우볶음",
  "cuisine": "한식",
  "situation": ["간편식"],
  "type": "볶음",
  "flavor": ["담백"],
  "main": ["애호박", "새우"],
  "time": 20,
  "servings": 2,
  "calories": 310,
  "nutrition": { "carb": 24, "protein": 18, "fat": 12 },
  "ingredients": [
    { "name": "애호박", "amount": "1개" },
    { "name": "칵테일새우", "amount": "150g" }
  ],
  "steps": [
    "애호박은 반달 모양으로 0.5cm 두께로 썬다.",
    "팬에 기름을 두르고 다진 마늘을 약불에 볶아 향을 낸다."
  ]
}
```

모든 필드는 필수다. 하나라도 비우지 않는다.

### 6.2 태그 어휘 (닫힌 목록 — 여기 없는 값을 쓰지 않는다)

| 필드 | 개수 | 허용값 |
|---|---|---|
| `cuisine` | 1개 필수 | 한식 · 양식 · 중식 · 일식 · 베트남식 · 태국식 · 간식 |
| `situation` | 0개 이상 | 술안주 · 건강식 · 아이들요리 · 간편식 |
| `type` | 1개 필수 | 밥 · 국찌개 · 볶음 · 무침 · 조림 · 구이 · 튀김 · 면 · 샐러드 · 빵디저트 · 음료 |
| `flavor` | 1~2개 | 매콤 · 얼큰 · 담백 · 고소 · 새콤 · 달콤 · 짭짤 |
| `main` | 1~3개 | 자유. 단 **일반명**으로 적는다 (칵테일새우 ✗ → 새우 ○) |

`main` 은 재고와 매칭되는 축이므로 일반명을 쓰고, 구체적인 품목명과 용량은 `ingredients` 에만 적는다.

### 6.3 계열 비율 (합계 1000)

| 계열 | 개수 |
|---|---|
| 한식 | 420 |
| 양식 | 140 |
| 중식 | 110 |
| 일식 | 110 |
| 베트남식 | 60 |
| 태국식 | 50 |
| 간식 | 110 |
| **합계** | **1000** |

### 6.4 상황 태그 최소 보유량

상황 태그는 계열과 무관하게 겹쳐 붙는다. 아래는 최소 개수다.

| 상황 | 최소 |
|---|---|
| 간편식 (조리 15분 이하) | 250 |
| 술안주 | 150 |
| 아이들요리 | 150 |
| 건강식 | 150 |

### 6.5 생성 규칙

- 요리명은 1000개 전체에서 중복되지 않는다
- 배치로 나눠 생성하되, 배치마다 계열 할당량을 지킨다
- `steps` 는 문장형, 단계당 한 문장, 4~8단계
- `ingredients` 는 3~12개, 전부 용량 표기

## 7. 디자인

### 7.1 색상 토큰

목업 1차안이 전반적으로 어두워서 밝게 조정한 값이다.

```css
--paper:      #F5F8F3;   /* 배경 */
--surface:    #FFFFFF;   /* 카드·패널 */
--ink:        #1B2620;   /* 본문 */
--ink-soft:   #6E7D73;   /* 보조 텍스트 */
--line:       #E5EBE1;   /* 구분선·테두리 */
--green:      #3E7D52;   /* 구조·보유 표시 */
--omija:      #C4394F;   /* 저장(♥)·폐기 확인 전용 */
--amber:      #DD9B33;   /* 조리순서 번호 */
```

### 7.2 서체

- 제목·요리명: `Gowun Batang` (700)
- 본문·UI: `IBM Plex Sans KR` (300/400/500/600)

### 7.3 표기 규칙 (어기지 않는다)

- 홈 카드는 **요리명 · 조리시간 · 칼로리만**. 재료 충족도 수치 금지
- `●`/`○` 는 보유 여부만 뜻한다. 수량 비교가 아니다
- 오미자색은 **저장(♥)과 폐기 확인 다이얼로그에만** 쓴다
- 탭 바와 입력창은 스크롤해도 상단 고정
- 재고는 항상 가나다순

## 8. 배포

- GitHub Pages (무료)
- PWA — 아내 폰 크롬에서 URL 열고 "홈 화면에 추가"
- 상태는 localStorage. 동기화 없음

## 9. 다음 단계 (2차)

- **홈 카테고리 필터**: 홈 추천 카드 위에 카테고리 칩 줄을 두고, 선택하면 그 카테고리에서 5개를 뽑는다
  칩 구성: 전체 · 한식 · 양식 · 중식 · 일식 · 베트남식 · 태국식 · 간식 · 술안주 · 건강식 · 아이들요리 · 간편식
  자연어 입력과 칩은 AND로 결합하고, 결과가 5개 미만이면 자연어 조건부터 완화한다
  데이터는 1차부터 이 필터를 지원하는 형태로 들어간다
